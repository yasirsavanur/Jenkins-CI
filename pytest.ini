[pytest]
addopts = -ra
markers =
    single: run the single suite (similar to TestNG single.xml)
    mobile: run the mobile suite (lightweight Chrome mobile emulation)
    regression: run all regression tests
